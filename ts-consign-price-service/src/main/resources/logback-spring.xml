<?xml version="1.0" encoding="UTF-8"?>
<configuration>
    <!-- Console appender with New Relic JSON encoder -->
    <appender name="STDOUT" class="ch.qos.logback.core.ConsoleAppender">
        <encoder class="com.newrelic.logging.logback.NewRelicEncoder"/>
    </appender>

    <!-- New Relic async appender for automatic log forwarding -->
    <appender name="ASYNC" class="com.newrelic.logging.logback.NewRelicAsyncAppender">
        <appender-ref ref="STDOUT"/>
    </appender>

    <!-- Root logger -->
    <root level="INFO">
        <appender-ref ref="ASYNC"/>
    </root>

    <!-- Application specific loggers -->
    <logger name="consignpriceservice" level="DEBUG"/>
    <logger name="consignpriceservice.controller" level="DEBUG"/>
    <logger name="consignpriceservice.service" level="DEBUG"/>
</configuration>
